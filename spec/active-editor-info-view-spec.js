'use babel';

import ActiveEditorInfoView from '../lib/active-editor-info-view';

describe('ActiveEditorInfoView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
